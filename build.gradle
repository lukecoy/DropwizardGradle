apply plugin: 'application'

mainClassName = "com.lukecoy.hello.HelloApplication"

project.ext {
    dropwizardVersion = '0.8.2'
    guiceDropwizardVersion = '0.8.2.0'
}

dependencies {
    compile project(':hello-health')
    compile project(':hello-resource')
}

allprojects {
    group = 'com.lukecoy'
    version = '1.0-SNAPSHOT'

    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    dependencies {
        compile group: 'com.hubspot.dropwizard', name: 'dropwizard-guice', version: guiceDropwizardVersion
        compile group: 'io.dropwizard', name: 'dropwizard-core', version: dropwizardVersion
    }
}

run {
    args 'server', 'config.yaml'
}
